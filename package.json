{
  "name": "apollo-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev:client": "bun --filter 'client' dev",
    "dev:server": "bun --filter 'server' dev",
    "dev": "bun --filter '*' dev",
    "clean": "bun pm cache rm && bun install --force"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "graphql": "^16.10.0"
  }
}
